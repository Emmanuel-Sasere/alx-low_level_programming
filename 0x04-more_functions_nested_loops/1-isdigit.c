#include "main.h"
/**
 * _isdigit - This is a function that checks for a digit (o through 9)
 * @c: inter to be checked
 * Return: 1 if c is a digit, 0 otherwise
 */
int_isdigit(int c)
{
return (c >= '0' && c <= '9');
}
