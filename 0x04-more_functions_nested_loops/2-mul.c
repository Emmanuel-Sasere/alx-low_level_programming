#include "main.h"
/**
 * mul - This is  a function tht multiplies two intergers.
 * @a: a - variable
 * @b: b - variable
 * Return: Always 0.
 */
int mul(int a, int b)
{
int mul;

mul = a * b;
return (mul);
}
